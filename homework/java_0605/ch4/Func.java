package homework.java_0605.ch4;

public class Func {
    static int count = 0;
    final static int MAX_CARS = 100;
    Model[] carlist = new Model[MAX_CARS];

    public static void addDate(scanner)
    {
        if(count < MAX_CARS){
            System.out.println("자동차 이름을 입력하세요");
            String carname = scanner.nextLine();
        }
    }
}
